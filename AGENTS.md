# Gakumas VR workspace rules

이 저장소의 VR 모드 작업에는 다음 규칙을 적용한다.

## 문서 동기화 — 마일스톤 완료 시

진행 중인 마일스톤에서는 `vrmod/`의 소스, 설정, 설치 스크립트, 테스트 또는 런타임 동작을 수정할 때마다 문서를 갱신하지 않는다. 중간 버전과 실기 결과는 실제 코드·설치 해시·런타임 로그 및 사용자에게 보고한 판정으로 추적하고, 해당 마일스톤이 달성되는 시점에 문서를 한 번에 동기화한다.

1. 런타임 동작이 바뀌면 문서 갱신 여부와 무관하게 `Entrypoint.cs`와 프로젝트의 버전을 함께 올린다.
2. 빌드 성공, 설치 성공, PC 실기, VR 실기는 서로 다른 상태로 판정한다. 사용자 실기 전에는 기능을 “검증 완료”로 간주하지 않는다.
3. 테스트가 중지된 상태에서 추측으로 결함을 해결 처리하지 않는다.
4. 개발 단계와 완료 조건은 `docs/VR_MILESTONES.md`를 따른다. 일부 성공이나 빌드만으로 달성을 선언하지 않는다.
5. 모든 조건이 사용자 VR 실기와 필요한 로그로 확인되면 같은 응답에서 반드시 **“마일스톤 Mx 달성”**이라고 알리고 다음 문서를 즉시 일괄 갱신한다.
   - 현재 설치본·검증 상태·미해결 문제: `docs/GAKUMAS_VR_STATUS.md`
   - 구조, 렌더 경로, 안전 원칙과 기술 결정: `docs/GAKUMAS_VR_DESIGN.md`
   - 해당 마일스톤 동안의 런타임 버전 변경: `vrmod/CHANGELOG.md`
   - 완료 조건과 달성 근거: `docs/VR_MILESTONES.md`
   - 다음 세션용 상세 이력과 현재 해시: `docs/VR_HANDOFF.md`
6. 마일스톤 완료 전에는 위 문서들을 중간 상태로 갱신하지 않는다. 사용자가 문서화 전용 감사나 별도 문서 갱신을 명시적으로 요청한 경우에만 예외로 한다.

문서의 현재 기준점은 v0.141.0, 진행 마일스톤은 M5다. M2는 v0.101.0 재진입/portrait 복귀, M3는 v0.131.0 시각·UI, M4는 v0.141.0의 game-rate stereo와 near-120Hz OpenXR submit 사용자 승인을 근거로 달성했다. 영상 v0.131, UI v0.90, 성능 v0.141을 회귀 기준으로 유지한다. M5에서는 메인 홈, 진행 중 메뉴/선택, 커뮤니케이션과 영상 재생의 실제 Camera/Canvas/RenderTexture/VideoPlayer 경로와 표시 정책을 조사하고, M6에서 승인된 비-live VR 범위를 구현한 뒤 M7 제품화로 진행한다. 정확한 90/120 stereo 생산은 비차단 선택 목표이며, 이벤트 기반 장면·카메라·UI 전환의 1~10ms 반응 목표와 1ms 전체 객체 열거 금지는 유지한다.

## 안전과 공존

- `GameAssembly.dll`, `UnityPlayer.dll`, 원본 `version.dll`, 게임 에셋을 수정하지 않는다.
- Localify 한글 패치의 번역, 폰트, 텍스처와 설정을 보존한다.
- 게임 실행 중 런타임 DLL을 교체하지 않는다.
- 설치 전 기존 런타임을 `vrmod/rollback/`에 보관한다.
- 실패 시 게임 자체는 창모드로 계속 실행되어야 하며, VR만 평면 패널로 폴백해야 한다.
- 로그에 계정 식별자, viewer ID, token 또는 실행 인증 정보를 남기지 않는다.

## 현재 보류 사항

- 실기는 사용자가 버전 실행을 알린 뒤에만 판정한다. 빌드/설치 상태를 사용자 실기 완료로 간주하지 않는다.
- 사용자는 향후 모든 안정성 전용 테스트를 생략하도록 지시했다. 지속 시간, 반복 횟수, 장시간 자원 수명과 누수 추세는 완료 조건으로 요구하지 않고 비차단·미검증으로 남긴다. 실제 관측된 크래시와 회귀는 계속 결함으로 취급한다.
- 스킬 사용 중 최상위 2D 이미지 누락 문제는 사용자가 해당 디버깅을 요청할 때만 착수한다.

## 세션 인수인계

- 새 VR 개발 세션은 `docs/VR_HANDOFF.md`, `docs/GAKUMAS_VR_STATUS.md`, `docs/GAKUMAS_VR_DESIGN.md`, `vrmod/CHANGELOG.md`를 먼저 읽고 실제 소스·설치 해시·최신 로그와 다시 대조한다.
- 세션 히스토리와 상세 실패 기록은 `VR_HANDOFF.md`에 두고 이 파일에는 영구 규칙만 둔다.
- 기능 작업 전 게임 핵심 파일 기준선과 Git metadata 존재 여부를 확인한다. 기준선 불일치나 `.git` 부재를 clean/검증 완료로 간주하지 않는다.
- 사용자가 문서화 전용 감사를 명시한 경우에는 runtime 코드를 바꾸지 않고 관측된 미해결 상태를 문서에 기록할 수 있다. 이때 결함을 해결 처리하지 않는다.
